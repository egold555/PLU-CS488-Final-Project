package notes;

public class WRITEUP {

	/*
	 
	 Ping http://google.com/404 <---- Because its less data returned, google.com has a bunch of JS
	 http://ipinfo.io/json <--- GeoIP. Must be HTTP because some proxies can't do https
	 
	 Some proxy servers are outside the 65536 port range. I am just ignoring them because I can't test them.
	 
	 */
	
}
